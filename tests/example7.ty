
def prueba(cond: int):
    if cond != 0:
        limit: int = cond
        while limit > 0:
            print(limit)
            limit = limit - 1
    else:
        print("limite debe ser mayor que 0")
        
        cond = 0

prueba(5)