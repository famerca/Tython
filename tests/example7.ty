
def prueba(cond: Int):
    if cond != 0:
        limit: Int = 0
        while limit > 0:
            limit = limit - 1
    else:
        print("limite debe ser mayor que 0")
        
        cond = 0

prueba(5)